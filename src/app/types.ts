export type PatientData = {
  id: number;
  firstName: string;
  lastName: string;
  dateOfBirth: string;
  email: string | null;
  isActive: boolean;
  createdAt: string;
  updatedAt: string | null;
};
